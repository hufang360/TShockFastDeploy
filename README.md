# 快速开服（FastDeploy）

可用指令
```
/fd init，快速处理（开服通用权限和设置）；
/fd perm，设置常用权限，并新增GM超管组；
/fd allow <指令 | journey | warp add>，授权使用某指令；
/fd del <指令>，取消指令授权"
/fd <guest | default>，设置插件的默认组为 guest 或 default"；
/fd reload, 配置重载；
```